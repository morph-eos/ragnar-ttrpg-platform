# Environment Configuration Template
# Ragnar TTRPG Platform - Cloud Infrastructure

# ============================================================================
# DOMAIN CONFIGURATION
# ============================================================================
# Your domain name without protocol (e.g., example.com)
# This domain must have DNS A record pointing to your server's IP address
DOMAIN=yourdomain.com

# Email address for Let's Encrypt certificate registration
# This email will receive notifications about certificate expiration
# and important security updates from Let's Encrypt
CERTBOT_EMAIL=admin@yourdomain.com

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# External MySQL/MariaDB database configuration
# Using external database provides better performance and scalability
# compared to containerized database solutions

# Database server hostname or IP address
# Can be a cloud database service (AWS RDS, Google Cloud SQL, etc.)
# or a dedicated database server
MYSQL_HOST=your-mysql-host.example.com

# Database name for Nextcloud installation
# Must be created beforehand with appropriate character set (utf8mb4)
MYSQL_DATABASE=nextcloud

# Database username with appropriate permissions
# User should have SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX privileges
MYSQL_USER=nextcloud_user

# Database password - use a strong, randomly generated password
# Consider using a password manager or generator for security
MYSQL_PASSWORD=your-secure-database-password

# ============================================================================
# NEXTCLOUD ADMIN CONFIGURATION
# ============================================================================
# Initial administrator account for Nextcloud
# This account will have full administrative privileges

# Administrator username - avoid common names like 'admin' for security
NEXTCLOUD_ADMIN_USER=your-admin-username

# Administrator password - must be strong and secure
# Minimum 12 characters with mixed case, numbers, and special characters
NEXTCLOUD_ADMIN_PASSWORD=your-secure-admin-password

# ============================================================================
# SECURITY RECOMMENDATIONS
# ============================================================================
# 
# Database Security:
# - Use dedicated database user with minimal required privileges
# - Enable SSL/TLS for database connections if possible
# - Regularly update database server with security patches
# - Consider database connection encryption
# 
# Password Security:
# - Use unique, randomly generated passwords
# - Store passwords securely (password manager)
# - Enable two-factor authentication in Nextcloud after setup
# - Regularly rotate passwords
# 
# Network Security:
# - Ensure firewall only allows necessary ports (80, 443)
# - Consider using VPN for administrative access
# - Monitor access logs regularly
# - Keep server OS updated with security patches
# 
# Backup Strategy:
# - Regular backups of ../data directory
# - Test backup restoration procedures
# - Secure offsite backup storage
# - Document recovery procedures
